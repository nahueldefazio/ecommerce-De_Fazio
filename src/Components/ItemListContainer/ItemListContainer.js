import React, {Fragment} from 'react';

function ItemListContainer(props) {
    return (
        <Fragment>
            <h1>{props.exampleText}</h1>
        </Fragment>
    );
}

export default ItemListContainer;